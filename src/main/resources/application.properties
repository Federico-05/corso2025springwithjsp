spring.application.name=demo
# Percorsi JSP

spring.mvc.view.prefix=/WEB-INF/jsp/
spring.mvc.view.suffix=.jsp

# H2 in memoria
#spring.h2.console.enabled=true
#spring.datasource.url=jdbc:h2:mem:docentedb

# Configurazione del database PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/Corso2025
spring.datasource.username=postgres
spring.datasource.password=federicodb05
spring.datasource.driver-class-name=org.postgresql.Driver

# Configurazione di JPA
# Opzioni: none, update, validate, create
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Mostra le query SQL in console (utile per il debug)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Porta del server
server.port=8080

#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#server.port=8080


# http://localhost:8080/docenti/lista