spring:
  application:
    name:demo

  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp


  datasource:
    url: jdbc:postgresql://localhost:5432/CorsoYML2
    username: postgres
    password: federicodb05
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      show-sql: true

server:
  port: 8081